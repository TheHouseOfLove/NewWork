<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rela_main"
    android:layout_width="fill_parent"
    android:layout_height="@dimen/header_height"
    android:background="@color/common_green"
    android:paddingTop="@dimen/header_paddingTop"
    android:minHeight="@dimen/header_height">

    <RelativeLayout
        android:id="@+id/rela_left"
        android:layout_width="@dimen/header_item_width"
        android:layout_height="@dimen/header_item_height"
        android:layout_alignParentLeft="true">

        <ImageView
            android:id="@+id/img_back"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:layout_centerInParent="true"
            android:layout_marginLeft="12.5dp"
            android:scaleType="center"
            android:src="@mipmap/back_icon" />

        <TextView
            android:id="@+id/text_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerInParent="true"
            android:layout_marginLeft="@dimen/common_divider"
            android:clickable="true"
            android:textColor="@color/common_white"
            android:textSize="15.6sp" />
    </RelativeLayout>

    <!-- header的Title部分 -->
    <TextView
        android:id="@+id/title"
        android:layout_width="200dp"
        android:layout_height="@dimen/header_item_height"
        android:layout_centerHorizontal="true"
        android:ellipsize="end"
        android:gravity="center"
        android:singleLine="true"
        android:textColor="@color/common_white"
        android:textSize="@dimen/header_title_fontSize"
        android:textStyle="normal" />
    <!-- header的右侧部分 -->
    <RelativeLayout
        android:id="@+id/rela_right"
        android:layout_width="80dp"
        android:layout_height="@dimen/header_item_height"
        android:layout_alignParentRight="true"
        android:paddingRight="15dp">

        <TextView
            android:id="@+id/textView_header_right"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:textColor="@color/common_white"
            android:textSize="15sp" />

        <ImageView
            android:id="@+id/img_header_right"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_centerVertical="true"
            android:scaleType="center" />
    </RelativeLayout>

    <TextView
        android:id="@+id/txt_rednum"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_marginRight="4dp"
        android:layout_marginTop="4dp"
        android:gravity="center"
        android:text="0"
        android:textColor="@color/common_white"
        android:textSize="10sp"
        android:visibility="gone" />
</RelativeLayout>
